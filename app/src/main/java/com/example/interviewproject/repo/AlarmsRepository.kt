package com.example.interviewproject.repo

import com.example.interviewproject.model.Alarm

class AlarmsRepository {

    suspend fun add(item: Alarm) {
        TODO()
    }

    suspend fun getAll(): List<Alarm> = TODO()

    suspend fun remove(item: Alarm) {
        TODO()
    }
}